const getNotes = require('./getNotes')
const addNote = require('./addNote')
const editNote = require('./editNote')
const deleteNote = require('./deleteNote')
const refreshNotes = require('./refreshNotes')

module.exports = {
    getNotes,
    addNote,
    editNote,
    deleteNote,
    refreshNotes,
}